let arr=[1,2,3,4,5];
//console.log(arr.slice(1,3)); //start from slice(start,end-1)
//console.log(Array.isArray(arr));

const steps = ["open", "fill"]; 
const alias = steps; 
steps.push("submit"); 
console.log(alias.length);

console.log(`["pass", "fail"].indexOf("skip")`)